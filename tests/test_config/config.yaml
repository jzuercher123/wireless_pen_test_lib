exploits:
  credential_extraction:
    capture_duration: 20
  payload_delivery:
    default_duration: 10
    payload_types:
    - reverse_shell
    - malicious_script
  session_hijacking:
    max_packets: 100
general:
  interface: wlan0mon
  log_level: DEBUG
  report_directory: reports
scanners:
  auth_bypass_scanner:
    scan_duration: 10
  dos_scanner:
    scan_duration: 10
  encryption_scanner:
    scan_duration: 15
ui:
  theme: default
